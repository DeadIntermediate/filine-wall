# NPM Configuration for FiLine Wall
# This file resolves peer dependency conflicts with react-simple-maps

# Use legacy peer dependency resolution (npm 6 behavior)
# Required for react-simple-maps compatibility with React 18
legacy-peer-deps=true

# Prefer exact versions
save-exact=false

# Engine strict checking
engine-strict=false
